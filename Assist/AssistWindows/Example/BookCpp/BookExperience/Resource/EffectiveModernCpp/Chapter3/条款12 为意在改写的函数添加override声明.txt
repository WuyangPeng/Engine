条款12 为意在改写的函数添加override声明
	★	如果要改写动作真的发生，有一系列要求必须满足：
		1.	基类中的函数必须是虚函数。
		2.	基类和派生类中的函数名字必须完全相同（析构函数例外）。
		3.	基类和派生类中的函数形参型别必须完全相同。
		4.	基类和派生类中的函数常量性必须完全相同。
		5.	基类和派生类中的函数返回值和异常规格必须兼容。
		6.	基类和派生类中的函数引用饰词必须完全相同。
	★	为意在改写的函数添加override声明
	★	成员函数引用饰词使得对于左值和右值对象（*this）的处理能够区分开来。