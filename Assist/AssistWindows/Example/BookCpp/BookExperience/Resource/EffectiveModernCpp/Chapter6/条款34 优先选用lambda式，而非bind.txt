条款34 优先选用lambda式，而非std::bind
	★	 在C++14中，标准运算符模板的模板型别实参大多数情况下可以省略不写。
	★	 绑定对象的所有实参都是按引用传递的，因为此种对象的函数调用运算符复用了完美转发。
	★	 lambda式比起使用std::bind而言，可读性更好、表达式更强，可能运行效率也更高。
	★	 仅在C++11中，std::bind在实现移动捕获，或是绑定到具备模板化的函数调用运算符的对象的场合中，可能尚有余热可以发挥。